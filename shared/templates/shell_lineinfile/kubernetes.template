# platform = multi_platform_ocp,multi_platform_rhcos
# reboot = false
# strategy = restrict
# complexity = low
# disruption = low
{{{ kubernetes_machine_config_shell_file(path=PATH, file_permissions_mode='0640', parameter=PARAMETER, value=VALUE, no_quotes=NO_QUOTES) }}}
